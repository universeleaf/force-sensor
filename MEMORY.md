# 接手记忆（2026-09-11）

## 用户目标与当前入口

用户要求检查新结果中 friction 方向及 friction cone constraint，修好结果、清理无用文件、缩短文件夹名，并留下本文件供新窗口接手。

仓库：`C:\Users\wty05\Desktop\gatech\force sensor`。MATLAB：`D:\bin\matlab.exe`。需要 Optimization Toolbox 的 `fmincon`。本地 `LCP-Continuum/` 是外部依赖，本次没有改它。

根目录 `force.m` 现在运行当前杆—平面实验：`force()` / `force('wall')` 或 `force('senior')`。历史 Gaussian 示例原 `force.m` 已原样移至 `legacy/force.m`。不要 `addpath(genpath(pwd))`，否则 legacy 同名函数可能遮蔽当前入口；只加 `rod`，主程序自行加依赖。

正式输出分别在 `out/wall/` 和 `out/senior/`。先看 `friction.png`、`forces.mp4`、`summary.txt`；完整数据在 `results.mat`，变量名 `results`。

## 问题原因及修改

1. 接手时主脚本已有用户未提交修改（约 211 行），其中 `previousShapeInCurrentFrame` 意外嵌套在另一个局部函数内，调用时报未定义。并且把上一帧世界坐标形状随当前基座移动，会从接触相对位移中减掉命令运动。已去掉该辅助函数；摩擦位移直接使用旧世界坐标。为线性化 Jacobian 对旧形状做的基座坐标变换仍保留，二者用途不同。
2. 原粘滑位移阈值 `0.05 mm` 大于内部滑动步长 `0.02 mm`，导致学长场景真实滑动被判为粘着。旧数据最后四帧摩擦沿 +x，估计摩擦功约 `+0.082 N mm`，但仍在圆锥内。新阈值上限 `0.005 mm`，有切向基座步进时再取与 `0.25*step` 的较小值。模式使用测得曲率重建的形状位移，不使用真值力。
3. 原切向基底按法向绝对值最小的坐标选参考轴。在 `[0,0,-1]` 附近，`1e-7` 扰动就可能切换 x/y 参考轴，导致摩擦方向编号转动 90°。现在 `contact_tangent.m` 按法向主轴选择下一循环坐标轴，保证当前两个轴向平面附近的局部连续性。一般跨主轴分区的任意法向轨迹仍需连续传输基底，不能宣称这是球面上全局连续的坐标图。
4. 保留并补全用户增加的约束：粘着分支也必须有 `w >= 0`。只检查 `beta.*w=0` 或圆锥范数不够。
5. 正向 LCP 每个内部步检查原始、未缩放的 `x`、`w=M*x+g`：有限性、非负性（容差 `1e-7`）及互补积（`1e-6`）。退化问题有时返回 ray/status 2，但原始残差已接近机器精度，因此验收按方程残差，不能只看状态码。
6. 修正正向多接触的法向分块索引，以及诊断循环中 `lambda` 数组被标量覆盖的问题。
7. `validate_friction.m` 独立重建 `D*beta`，检查力方向、圆锥、多面体锥、`w` 非负及摩擦功；总验证还要求最大形状 RMSE `<0.5 mm`，防止“约束可行但形状很差”被当作成功。该形状阈值是当前无噪声场景的数值验收值。
8. 图/视频拆分法向力、摩擦力和末端力，标明力作用在杆上、粘滑模式，以及杆端接触的可辨识性限制。`friction.png` 展示有符号摩擦、锥利用率、世界坐标接触步进和摩擦功。CSV 的 `w` 单位改为 mm。
9. 最终验收还发现检查器将稀疏位置样点线性连成弦，误把空间插值误差当成时间位移：wall 第 17 帧会算出约 `0.00515 mm` 假滑移，而曲率重建位移约 `0.00038 mm`。检查和作图改用 `measurements.p`（由测得曲率积分），仍独立于估计力生成的 `ours.p`。阈值不变。主程序现在先验收逆解，再计算耗时的 Aloi 对照。

## 坐标、约束与模型边界

全部力指平面对杆的力，世界坐标，单位 N；位置、弧长、单步接触位移使用 mm。`n` 是障碍物指向可行空间的单位法向；接触力 `f = n*fn + D*beta`，`fn>=0`、`beta>=0`。本项目准静态 LCP 用位移而非速度，`lambda` 和 `w` 的单位也是 mm。

```text
g = n'*(p_contact - p_plane) >= 0
g * fn = 0
w = D' * v_t + lambda * ones(m,1) >= 0
beta .* w = 0
coneSlack = mu*fn - sum(beta) >= 0
lambda >= 0; lambda * coneSlack = 0
```

`m=16` 为切平面多面体近似。`||ft||<=mu*fn` 是必要检查，但相反方向的 beta 能互相抵消，所以不能替代 `sum(beta)<=mu*fn`。滑动时应检查 `ft' * v_t <= 0`；静摩擦不要求固定沿某个世界轴的负向。物理背景可参照 [Drake friction model](https://drake.mit.edu/doxygen_cxx/group__friction__model.html)。

正向算法先线性化再更新非线性形状，粘着帧可能有约 `0.001 mm` 的非线性漂移。不能把这点漂移当作宏观滑动；也不能把 `0.02 mm` 的实际步进用 `0.05 mm` 阈值掩盖。验证同时保留模型位移和观测位移，避免只用估计形状自证。

逆估计不输入真值力；当前曲率由 24 个无噪声 FBG 样点插值。但 `measurements.previousShape = forward.previousState`，即每个输出帧使用**紧邻的前一内部仿真步的稠密形状**，而不是前一输出帧或真实传感器历史。位置样点也来自模拟测量。现阶段是理想化物理一致性验证，不能宣称已经验证仅靠实际低频 FBG 测量的在线系统；真实实验必须提供相同时间尺度的形状历史或重新定义离散时间模型。

## 两个正式场景

共同：杆长 150 mm，保留原始预曲率（积分弯曲约 88.49°），无噪声传感，先无摩擦推进，再 `mu=0.5` 横移，真实末端力为 0，但逆状态仍保留未知三维末端力。

| 场景 | 平面/运动 | 输出帧 | 解释 |
|---|---|---:|---|
| wall | 平面 x=20，n=-x；+x 推进 45 mm，+z 横移 1 mm | 18 | 最终接触约 s=125.9 mm，与末端分离；横移过程可保持粘着 |
| senior | 平面 z=50，n=-z；+z 推进 40 mm，+x 横移 20 mm | 10 | 最终 s=150 mm；最后四帧滑动，摩擦应沿 -x |

**学长场景的分量幅值不能唯一恢复。** 接触点与末端重合时，相同位置的接触力和独立末端力只通过和进入杆的平衡式。当前修复结果最后真实接触力约 `[-3.965,0,-7.930] N`；估计接触力约 `[-0.113,0,-0.225] N`，其余由未知末端力承担。方向及约束正确，合力准确，但不能把这说成两个分量均已估准。若后续必须识别分量，需要明确加入已知零末端力的实验信息，或改变接触位置/观测；不要为了图好看偷偷把真值末端力塞入逆求解。

Aloi 对照维持原有非线性 Gaussian 平衡迭代。其局部轴向载荷固定为零；当前最终真值的局部轴向分量占比约 wall 24.59%、senior 99.63%。因此图中的全向量误差是此场景诊断，不是论文方法在适用条件下的性能结论。不得直接翻转其向量或改成真值方向。

## 验证及复现

```matlab
addpath('rod');
test_friction_regression();               % 两个正式 MAT + 基底扰动 + 故意锥违规
validate_rod_plane_displacement_forward();
test_reverse_slide();                    % -x 滑动 2 mm，内部步长 0.01 mm
```

反向场景在稀疏输出帧附近跨越粘滑切换，默认 2 次 EKF / 25 次 SQP 曾因形状误差失败；该回归显式允许 6 / 60 次迭代，原验收标准保持不变。通过时最大形状 RMSE `0.043 mm`、总力轨迹 RMSE `0.01915 N`、最终相对误差 `0.03604%`，滑动帧为 5、6，正摩擦功为 0。不能据此声称任意路径默认迭代预算都足够。

基底测试在旧实现上确实失败；旧学长 MAT 被新方向检查拒绝。故意向相反 beta 分量同时加正数的测试保持合摩擦力不变，但必须被多面体锥检查拒绝。

最新正式验收数值、图像及视频检查见 [docs/validation.md](docs/validation.md)。当前 MAT 自带 `results.validation`。只重新出图/视频可调用：

```matlab
run_rod_plane_force_sensing_experiment( ...
    'render',fullfile(pwd,'out','senior','results.mat'));
```

`render` 会先重验物理约束、更新输出目录和指标，再覆盖该目录的结果/图/视频。`cfg.diagnostics.stopAfterInverse=true` 用于只验证逆求解，省略耗时的 Aloi 和出图；返回结果带 `diagnosticOnly=true`，不等同于正式完整结果。

学长最终结果的 Aloi 对照复用了本次已算完的完整预算优化，合并前逐项断言新旧正向形状、曲率测量和真值力一致到 `1e-10`。修正逆解来自严格 fmincon 运行，未采用那次用于快速诊断的低迭代 Aloi。wall 最后也将严格逆解和独立完整 Aloi 计算分开保存、核对输入后合并，以免验收失败丢掉耗时计算。MAT 中的 `provenance` 记录来源，合并脚本和日志保存在诊断归档。完整预算不等于已证明全局最优；优化退出码和非线性平衡残差都保留在结果里。

## 清理和版本注意事项

- 当前代码在 `rod/`；公开函数名保留。旧 `force_outputs/` 改为 `out/`，每个场景仅保留一套短文件名的正式输出。
- 历史示例在 `legacy/`，论文/公式在 `papers/`，会议材料在 `docs/`。原长说明文档在 `archive/old-notes.zip`。
- `archive/before-fix.zip` 保存接手时完整当前实验目录、原输出和 README，包括用户原未提交修改。`archive/old-outputs.zip` 是原根目录已有的输出压缩包。旧输出在 `archive/old-out/`，临时输出在 `archive/checks/`，诊断脚本和日志在 `archive/diagnostics/`。历史脚本保留当时路径；正常复现以本文件的当前入口命令为准。
- 原 `rod_plane_force_sensing/` 已清空，但目录仍被其他进程占用，原生移动也返回 sharing violation，暂留在根目录。没有结束用户进程。删除操作被工具自动审核拒绝（仅返回 blocked by policy），因此其余清理采用可恢复的归档移动；当前入口没有自动删除目录的副作用。释放相关编辑器/终端/应用的目录句柄后可手动删掉该空目录，代码不再依赖它。
- `.gitignore` 忽略 `tmp/`、`out/*_tmp/`、压缩包、PDF、外部依赖和本地配置。归档/PDF/依赖均留在本机，Git 克隆不能自动恢复这些内容；迁移工作区时需一起复制。
- 接手基线为 `b33f5d6`；本次没有提交或推送。Git 中大量删除/新增对应目录搬迁，不要据此恢复旧输出覆盖新结果，也不要覆盖用户原有修改。

后续先读本文件和验证记录，再查看 `rod/run_rod_plane_force_sensing_experiment.m` 的 `selectComplementarityMode`、`activeSetMapConstraints`、`decodeMapState` 和 `rod/validate_friction.m`。不要重复扫描归档和外部依赖，也不要把约束可行性、总力精度、分量可辨识性混为一谈。
